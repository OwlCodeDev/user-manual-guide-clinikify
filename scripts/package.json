{
  "name": "clinikify-manual-screenshots",
  "private": true,
  "type": "module",
  "description": "Captura automatizada dos prints do manual (Playwright).",
  "scripts": {
    "capture": "node capture.mjs",
    "install:browser": "playwright install chromium"
  },
  "dependencies": {
    "playwright": "^1.49.0"
  }
}
